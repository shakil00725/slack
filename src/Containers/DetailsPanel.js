import React from 'react';
import DetailsPanel from '../components/MetalPannel/MetalPanel';

const DetailsContainer = ({channel}) => (
    <DetailsPanel channel={channel} />
  );
  

export default DetailsContainer;